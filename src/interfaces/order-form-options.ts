export interface ReceiverOptions {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
}

export interface ShippingOptions {
  address: string;
  city: string;
  country: string;
  state: string;
  postalCode: string;
}
